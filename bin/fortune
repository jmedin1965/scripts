#!/bin/sh

fortune="/usr/games/fortune"
for fortune in /usr/bin/fortune.exe /usr/games/fortune /usr/bin/fortune /bin/fortune
do
    if [ -x "$fortune" ]
    then
        exec "$fortune"
    fi
done

echo "$fortune: app not found."
echo "install with: sudo apt install fortune-mod fortunes"

