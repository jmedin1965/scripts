Content-Type: text/x-zim-wiki
Wiki-Format: zim 0.6
Creation-Date: 2024-12-02T15:22:58+11:00

====== readme ======
Created Monday 02 December 2024

===== sbin/add_bareos_repositories.sh =====
Need to add description

===== sbin/auto-mount.sh =====
Need to add description

===== sbin/backup.sh =====
Need to add description

===== sbin/bareos-add-client.sh =====
Need to add description

===== sbin/bareos-create-db-user.sh =====
Need to add description

===== sbin/bareos-drop-db-user.sh =====
Need to add description

===== sbin/bareos-mail-report.sh =====
Need to add description

===== sbin/bw =====
Need to add description

===== sbin/detect-apt-cacher-proxy.sh =====
Need to add description

===== sbin/dhcp-POSIX-Time.sh =====
script to work out dhcp options 100 and 101 for you, or at least make it easier. Probably needs some work for certain timezones

example:
 
	dhcp-POSIX-Time.sh  "Australia/NSW" "Australia/Brisbane" ...

===== sbin/filesystem-merge-squashfs.sh =====
Need to add description

===== sbin/findoid =====
Need to add description

===== sbin/fix-home-mnt.sh =====
Need to add description

===== sbin/foreman-apt-install-force.sh =====
Need to add description

===== sbin/foreman.pl =====
Need to add description

===== sbin/functions.sh =====
Need to add description

===== sbin/get.cam.sh =====
Need to add description

===== sbin/getssl-auto-renew =====
Need to add description

===== sbin/git-check.sh =====
Need to add description

===== sbin/git-pull-local.sh =====
Need to add description

===== sbin/go-upgrade.sh =====
Need to add description

===== sbin/install-puppet-agent.sh =====
Need to add description

===== sbin/local-functions =====
Need to add description

===== sbin/netapp-cluster-send-command =====
Need to add description

===== sbin/netapp-cluster-send-command-do-hourly =====
Need to add description

===== sbin/netapp-cluster-send-command-enable-ssh-key =====
Need to add description

===== sbin/netapp-sector-520.sh =====
Need to add description

===== sbin/pfs-backup-config.sh ===== is missing ===== is missing
Need to add description

===== sbin/proxmox-backup-turn-off.sh =====
Need to add description

===== sbin/proxmox-backup.sh =====
Need to add description

===== sbin/proxmox-setup.sh =====
Need to add description

===== sbin/puppet-clear-agent-cert.sh =====
Need to add description

===== sbin/pve-syncoid =====
Need to add description

===== sbin/pveum_user_password.sh =====
Need to add description

===== sbin/readme.txt =====
Need to add description

===== sbin/recycle-shadow-copy.sh =====
Need to add description

===== sbin/sanoid =====
Need to add description

===== sbin/sanoid-hook =====
Need to add description

===== sbin/sanoid-hook-proxmox =====
Need to add description

===== sbin/sanoid-sync =====
Need to add description

===== sbin/syncoid =====
Need to add description

===== sbin/terraform-upgrade.sh =====
Need to add description

===== sbin/update-bw =====
Need to add description

===== sbin/update-sanoid =====
Need to add description

===== sbin/update-scripts-fine-grained-key =====
Need to add description

===== sbin/vm-convert-disk-to-lxc.sh =====
Need to add description

===== sbin/vm-disk-swap.sh =====
Need to add description

===== sbin/vm-migrate.sh =====
Need to add description

===== sbin/vm-mount.sh =====
Need to add description

===== sbin/vm-set-refquota.sh =====
Need to add description

===== sbin/vm-umount.sh =====
Need to add description

===== sbin/vm-vmdk-mount.sh =====
Need to add description

===== sbin/vm-vmdk-umount.sh =====
Need to add description

===== sbin/wsl-commands.sh =====
Need to add description

===== sbin/wsl-crontab-commands.sh =====
Need to add description

